/**
 * Shared constants used across UI surfaces and the background service worker.
 */

export const GROUP_COLORS = Object.freeze([
  'blue', 'purple', 'teal', 'red', 'orange', 'pink', 'indigo', 'yellow', 'slate',
]);
